CREATE TABLE USER2(login varchar(32) NOT NULL PRIMARY KEY, password varchar(256), verify_token varchar(32), salt varchar(32), mail varchar(128), create_time TIMESTAMP);
CREATE TABLE SESSION(login varchar(32) NOT NULL, token varchar(32) NOT NULL, create_time TIMESTAMP);
